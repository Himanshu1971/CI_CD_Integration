workflows:
  flutter-build:
    name: "flutter-build"
    max_build_duration: 30
    instance_type: mac_mini_m2
    environment:
      flutter: stable
    scripts:
    - name: "say, hello"
      script: echo "Hello from codemagic"
